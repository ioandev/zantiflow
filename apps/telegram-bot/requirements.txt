# The shared protocol package (editable, from the monorepo).
-e ../../packages/notify-protocol
aiogram>=3.29
python-dotenv>=1.2

# Dev only (not in the Docker image): `just dev` auto-reload on file changes.
watchfiles>=0.24
